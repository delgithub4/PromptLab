version: "3.9"

services:

  promptlab:

    build: .

    container_name: promptlab-service

    restart: unless-stopped

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app
